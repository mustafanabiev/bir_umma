import 'package:flutter/material.dart';

class StartAppView extends StatelessWidget {
  const StartAppView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('StartAppView')),
    );
  }
}
